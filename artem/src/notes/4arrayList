ArrayList (коллекция) это
DEFAULT_CAPACITY = 10
(oldCapasity*3)/2+1

В непараметризированный ArrayList можно добавлять переменнуй любого типа.
ArrayList <Employee> employees = new ArrayList<>(); это параметризированный ArrayList

Методы ArrayList-а :
 -- .add добавляет значение для следующего незаполненной ячейки или в ячейку с обозначенным индекстом (при этом все заполненые ячейки смещаются в ПРАВО)
 -- .set этот метод замещает уже находящийся в  (метод работает с УЖЕ ЗАПОЛНЕННЫМИ ячейками)
 -- .get возвращает значение
 -- .size возвращает количество заполненных ячеек в коллекции
 -- .

СТУКТУРЫ данных:
 -- статическая (Простой массив)
 -- динамическая (пример ArrayList)
 -- меп
 -- множество

Все классы являются наследниками Object