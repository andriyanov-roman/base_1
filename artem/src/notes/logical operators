    -------Логические операции-------

        && — логическое и
        || — логическое или
        ! — логическое отрицание
        ^ — XOR


    ----- Операции сравнения ---------

        > — больше
        >= — больше или равно
        < — меньше
        <= — меньше или равно
        == — равно
        != — неравно